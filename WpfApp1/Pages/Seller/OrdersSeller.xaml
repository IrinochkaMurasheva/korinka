<Page x:Class="WpfApp1.Pages.OrdersSeller"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrdersSeller">

    <Grid>
        <ListView x:Name="listUsers" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Номер заказа" Width="100" DisplayMemberBinding="{Binding name}"/>
                    <GridViewColumn Header="Сумма" Width="100" DisplayMemberBinding="{Binding count}"/>
                    <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding price}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
